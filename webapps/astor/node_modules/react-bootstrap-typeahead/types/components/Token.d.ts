import { UseTokenProps } from '../behaviors/token';
interface TokenProps<T> extends UseTokenProps<T> {
    disabled?: boolean;
    readOnly?: boolean;
}
declare const Token: ({ readOnly, ...props }: TokenProps<HTMLElement>) => JSX.Element;
export default Token;
