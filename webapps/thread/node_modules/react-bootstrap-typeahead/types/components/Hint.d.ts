import React, { KeyboardEvent } from 'react';
export declare type ShouldSelect = (shouldSelectHint: boolean, event: KeyboardEvent<HTMLInputElement>) => boolean;
export declare function defaultShouldSelect(e: KeyboardEvent<HTMLInputElement>, shouldSelect?: ShouldSelect): boolean;
interface Config {
    children: JSX.Element;
    shouldSelect?: ShouldSelect;
}
export declare const useHint: ({ children, shouldSelect }: Config) => {
    child: React.FunctionComponentElement<any>;
    hintRef: React.MutableRefObject<HTMLInputElement | null>;
    hintText: string;
};
interface HintProps extends Config {
    className?: string;
}
declare const Hint: ({ className, ...props }: HintProps) => JSX.Element;
export default Hint;
