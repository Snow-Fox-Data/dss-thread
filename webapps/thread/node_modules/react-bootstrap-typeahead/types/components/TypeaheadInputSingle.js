import React from 'react';
import Hint from './Hint';
import Input from './Input';
import withClassNames from '../behaviors/classNames';
export default withClassNames(({ inputRef, referenceElementRef, shouldSelectHint, ...props }) => (React.createElement(Hint, { shouldSelect: shouldSelectHint },
    React.createElement(Input, { ...props, ref: (node) => {
            inputRef(node);
            referenceElementRef(node);
        } }))));
